@model IEnumerable<Registro_Visitas_Renso_Soriano.Data.Changes>

@{
    ViewData["Title"] = "Index";
}


<div class="d-flex align-items-center">
    <div class="flex-grow-1">
        <h1 class="mt-3">Lista de Cambios Registrados </h1>
    </div>
    <div>
        <a href="@Url.Action("Create")" class="btn btn-primary ">Registrar Nuevo Cambios</a>
    </div>
</div>

<div>
    <form method="get">

        <input name="Search" value="@ViewData["search"]" />
    </form>
    <p></p>
    </div>


<p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ChangesNames)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ChangesNames)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdChanges">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdChanges">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdChanges">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
